{% extends 'base.html.twig' %}

{% block title %}Rendu{% endblock %}

{% block body %}
    <h1>Rendu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendu.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ rendu.url }}</td>
            </tr>
            <tr>
                <th>DateHeure</th>
                <td>{{ rendu.dateHeure ? rendu.dateHeure|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rendu_index') }}">back to list</a>

    <a href="{{ path('app_rendu_edit', {'id': rendu.id}) }}">edit</a>

    {{ include('rendu/_delete_form.html.twig') }}
{% endblock %}
